datasource db {
    provider = "postgresql"
    url      = "postgres://postgres:xtuned272@localhost:5432/bankapp"
}

model User {
    id    Int    @id @default(autoincrement())
    name  String
    email String @unique
}